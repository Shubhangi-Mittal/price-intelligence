# ── requirements-cloud.txt ────────────────────────────────────────────────────
# Cloud / production dependencies.
# Only install these when you have real GCP / Airflow credentials.
#
# Usage:
#   pip install -r requirements.txt -r requirements-cloud.txt
#
# These are pinned tightly to avoid the pip backtracking problem caused by
# dbt-bigquery pulling in the full google-cloud ecosystem without constraints.
# ─────────────────────────────────────────────────────────────────────────────

# ── GCP storage + BigQuery ────────────────────────────────────────────────────
# Pin google-cloud-* together — they share grpc/protobuf deps and conflict
# badly when mixed versions are resolved independently.
google-auth==2.29.0
google-auth-httplib2==0.2.0
google-api-core==2.18.0
google-cloud-core==2.4.1
google-cloud-storage==2.16.0
google-cloud-bigquery==3.20.0
google-cloud-bigquery-storage==2.24.0
google-cloud-dataproc==5.10.2      # pinned low — dbt-bigquery only needs ~5.0
grpcio==1.62.2
grpcio-status==1.62.2
protobuf==4.25.3
proto-plus==1.23.0
opentelemetry-api==1.24.0
opentelemetry-sdk==1.24.0
opentelemetry-proto==1.24.0         # pin this — it's what causes the long backtrack

# ── dbt BigQuery adapter ─────────────────────────────────────────────────────
# Must come AFTER the google-cloud pins above so pip doesn't re-resolve them.
dbt-bigquery==1.7.7

# ── AWS (alternative to GCS/BigQuery) ────────────────────────────────────────
# Uncomment if using S3 + Redshift / Snowflake instead of GCP:
# boto3==1.34.69
# snowflake-connector-python==3.9.0
# dbt-snowflake==1.7.3

# ── Airflow ───────────────────────────────────────────────────────────────────
# Install Airflow with constrained deps using the official constraints file.
# This avoids ALL Airflow dependency conflicts. Run this instead:
#
#   AIRFLOW_VERSION=2.9.0
#   PYTHON_VERSION=3.11
#   pip install "apache-airflow==${AIRFLOW_VERSION}" \
#     --constraint "https://raw.githubusercontent.com/apache/airflow/constraints-${AIRFLOW_VERSION}/constraints-${PYTHON_VERSION}.txt"
#
# Do NOT add apache-airflow here without the constraints URL — it will conflict.

# ── Data quality ─────────────────────────────────────────────────────────────
great-expectations==0.18.15